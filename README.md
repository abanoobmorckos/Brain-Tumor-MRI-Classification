# Brain Tumor MRI Classification

Upload an MRI image and get tumor type prediction using a fine-tuned MobileNetV2 model.

## How to run locally

1. Clone the repo
2. Create a virtual environment
```bash
python -m venv brain-venv
source brain-venv/Scripts/activate  # Windows
# or
source brain-venv/bin/activate      # Linux/Mac
